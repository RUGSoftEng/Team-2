<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="rugse.team2.MeetGroningen" >

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />


    <application
        android:name=".DatabaseStuff.GlobalVariables"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name="rugse.team2.MeetGroningen.Activities.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="rugse.team2.MeetGroningen.Activities.ChooseQuestActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="rugse.team2.MeetGroningen.Activities.LandMarkPopUpActivity"
            android:parentActivityName="rugse.team2.MeetGroningen.Activities.ContinueQuestActivity"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity
            android:name="rugse.team2.MeetGroningen.Activities.ContinueQuestActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="rugse.team2.MeetGroningen.Activities.MakeQuestActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="rugse.team2.MeetGroningen.Activities.MapActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="rugse.team2.MeetGroningen.Activities.OnQuestActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="rugse.team2.MeetGroningen.Activities.QuestPreviewActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name="rugse.team2.MeetGroningen.Activities.UserPageActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="rugse.team2.MeetGroningen.Activities.MakeLandmarkActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="rugse.team2.MeetGroningen.Activities.ManageCustomActivity"
            android:theme="@style/AppTheme.NoActionBar" />

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name="rugse.team2.MeetGroningen.Activities.QuestFinishedActivity"
            android:label="@string/title_activity_quest_finished"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="rugse.team2.MeetGroningen.Activities.TutorialActivity"
            android:label="@string/title_activity_tutorial"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>


</manifest>